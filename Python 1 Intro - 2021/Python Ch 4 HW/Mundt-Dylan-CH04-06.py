#Set a range, then calculate the fahrenheit to celsius formula.
for currentCelsiusTemperature in range( 21 ): 
    fahrenheitTemperatureEquivalent = ( 9 / 5 ) * currentCelsiusTemperature + 32
#Print the resulting statement.
    print( currentCelsiusTemperature, "\t", fahrenheitTemperatureEquivalent )